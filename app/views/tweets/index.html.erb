<section class="section">
  <div class="container">
    <div class="columns">
      <% if current_user %>
        <%= render 'profile' %>
      <% else %>
        <%= render 'trends' %>
      <% end %>
      <%= render 'feed' %>
      <%= render 'whofollow' %>
    </div>
  </div>
</section>

